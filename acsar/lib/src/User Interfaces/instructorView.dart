import 'package:flutter/material.dart';

class Instructor {
  Widget instructorView(BuildContext context) {
    return Scaffold(
      body:
          Text('Instructor Dashboard'), //will be replaced by student interface
    );
  }
}
