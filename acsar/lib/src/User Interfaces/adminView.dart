import 'package:flutter/material.dart';

class Admin {
  Widget adminView(BuildContext context) {
    return Scaffold(
      body: Text('Admin Dashboard'), //will be replaced by admin interface
    );
  }
}
